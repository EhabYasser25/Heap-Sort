import CLI.SortApp;
import Heap.ArrHeap;

import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        SortApp app = new SortApp();
        app.startProgram();
//        ArrHeap heap = new ArrHeap(10);
//        heap.heapSort(new int[]{1, 2, 3, 4, 5});
//        for(int[] arr : heap.steps)
//            System.out.println(Arrays.toString(arr));

    }
}
