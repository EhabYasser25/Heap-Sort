package Heap;

public class MaxHeap {

    //Assuming we're working with integers. Change if we're told otherwise.
    private int[] heap;

    public void heapify(){

    }

    //Heapify the given list and save the resulting array in the heap field.
    public void buildHeap(int[] list){
    }

    //Return a sorted array of the elements currently in the heap
    public int[] heapSort(){

        return this.heap;
    }

    //Insert into the max heap
    public void insert(int element){

    }

    //Remove and return the max element from the max heap and adjust the heap accordingly.
    public int removeMax(){

        return 0;
    }
}
